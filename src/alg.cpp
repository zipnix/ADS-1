// Copyright 2021 NNTU-CS

int cbinsearch(int *arr, int size, int value) {
    int score=0;
  for (int i = 0; i <= size; i++) {
  if (arr[i] = value) {
    score++;
  }
  if (score != 0)
    return score;
  else
  return 0;
}
